@model HomeIndexVM


<div>
    <div>
        <select id="DropDownList">  
            @foreach (var item in Model.Skill)
            {
                <option>@item.Skill1</option>
            }
        </select>
        <select id="SelectList" multiple="multiple">
            @foreach (var item in Model.Person)
            {
                <option onclick="GetInformation(@item.Id)" id="item">@item.FirstName</option>
            }
        </select>
    </div>
    <div id="PersonInfo">
    </div></div>
